app:
  name: TestApp
  version: 1.0.1
  debug: false
  environment: production

server:
  host: 0.0.0.0
  port: 5000
  timeout: 30

database:
  type: sqlite
  name: test.db

logging:
  level: INFO
  file: app.log
